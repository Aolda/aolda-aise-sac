---
# KVM 계정 감시 플레이북
# 목적: KVM 호스트의 30일 이상 미사용 계정을 식별하고 감시 보고서 및 자동화 스크립트 생성

- name: "KVM 계정 보안 감시"
  hosts: kvm_hosts
  gather_facts: yes
  become: yes
  vars:
    apply_policy: false
  check_mode: "{{ not (apply_policy | bool) }}"
  
  roles:
    - kvm-account-audit
