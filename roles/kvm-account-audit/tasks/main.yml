---
- name: "KVM 계정 보안 정책 포함"
  ansible.builtin.import_tasks: accounts.yml
  tags:
    - kvm_accounts

- name: "KVM 세션 timeout 정책 포함"
  ansible.builtin.import_tasks: session_timeout.yml
  tags:
    - kvm_session_timeout

- name: "KVM IP 접근 제한 정책 포함"
  ansible.builtin.import_tasks: ip_restrict.yml
  tags:
    - kvm_ip_restrict
