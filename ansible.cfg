[defaults]
roles_path = roles:tests/fixtures/roles
local_tmp = /tmp/ansible-local
remote_tmp = /tmp/ansible-remote
callback_plugins = plugins/callback
callbacks_enabled = security_report

[callback_security_report]
report_dir = reports
report_title = 보안조치 결과 보고서
